namespace SpawnDev.SpawnJS
{
    public enum JSArrayBufferView
    {
        // big
        BigInt64Array,
        BigUint64Array,
        // float
        Float16Array,
        Float32Array,
        Float64Array,
        // int
        Int16Array,
        Int32Array,
        Int8Array,
        // uint
        Uint16Array,
        Uint32Array,
        Uint8Array,
        Uint8ClampedArray,
        // data view
        DataView,
        // non-view
        ArrayBuffer,
        SharedArrayBuffer,
    }
}
